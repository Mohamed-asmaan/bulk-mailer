# Local dev: copy to `.env` and use your backend (no trailing slash).
# VITE_API_URL=http://localhost:5000
#
# Production: `frontend/.env.production` sets the Railway API URL for `vite build`.
# To override on Vercel, add VITE_API_URL in Project → Settings → Environment Variables.
