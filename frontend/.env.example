# Optional override (no trailing slash). Dev server defaults to http://localhost:5000.
# Production builds default to https://bulk-mailer-production-c860.up.railway.app unless you set this.
VITE_API_URL=http://localhost:5000
