# Backend API base URL (no trailing slash). Local default is http://localhost:5000
# On Vercel: Project → Settings → Environment Variables → VITE_API_URL = your Railway URL
# Example: https://your-app.up.railway.app
VITE_API_URL=http://localhost:5000
