# Used automatically by `vite build` (e.g. Vercel). Public API only — no secrets.
# Override in Vercel → Environment Variables if you use a different backend URL.
VITE_API_URL=https://bulk-mailer-production-c860.up.railway.app
